 <nav class="navbar navbar-dark navbar-expand-md sticky-top navbar-shrink py-3" id="mainNav">
      <div class="container">
        <a class="navbar-brand d-flex align-items-center" href="/">
        <span>
            <img src="/img/logos/Z Zephyrus.png" width="180px">
        </span></a><button data-bs-toggle="collapse" class="navbar-toggler" data-bs-target="#navcol-1"><span class="visually-hidden">Toggle navigation</span><span class="navbar-toggler-icon"></span></button>
          <div class="collapse navbar-collapse" id="navcol-1">
              <ul class="navbar-nav mx-auto">
                  <li class="nav-item">
                    <a class="nav-link <%= path === '/' ? 'active' : '' %>"" href="/">Explore</a>
                  </li>
                  <li class="nav-item">
                    <a class="nav-link <%= path === '/events' ? 'active' : '' %>"" href="/events">Events</a>
                  </li>
                  <li class="nav-item">
                    <a class="nav-link <%= path === '/register' ? 'active' : '' %>" href="/register">Payment</a>
                  </li>
                  <% if (spotAccess) { %>
                    <li class="nav-item">
                      <a class="nav-link <%= path === '/spot-registration' ? 'active' : '' %>"" href="/spot-registration">Spot</a>
                    </li>
                  <% } %>
              </ul>             
              <% if (!isAuthenticated) { %>
                <a class="btn btn-primary shadow" role="button" href="/login_user" style="margin-top: 11px;">Login</a>
              <% } else { %>
                <div class="dropdown" style="padding-right: 0px;margin-right: 24px;">
                  <a class="dropdown-toggle" aria-expanded="true" data-bs-toggle="dropdown" href="#">
                    <% if (userImage) { %>
                      <img class="user-img" src="<%= userImage %>" referrerpolicy="no-referrer" alt="1" />
                    <% } else if (name) { %>
                      Welcome, <%= name %>
                    <% } else { %>
                      Welcome
                    <% } %>
                  </a>
                    <div class="dropdown-menu" data-bs-popper="none">
                      <a class="dropdown-item" href="/user-profile">Profile</a>
                      <a class="dropdown-item" href="/orders">Registrations</a>
                    </div>
                </div>
                <form action="/logout" method="post">
                  <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                  <button type="submit" class="btn btn-primary shadow" style="margin-top: 11px;">Logout</button>
                </form>
                
              <% } %>
          </div>
      </div>
  </nav>
